#Plugging into next.js test API
type IO {
    ID: ID!
    hello: String!
    Image: String
    Date: Date!
    Past: 
}

# Change localhost to dynamic path regex or next function
type Query {
    helloWorld: [IO]
        @rest(
            endpoint: "127.0.0.1:3000/api/hello"
            setters: [
                { field: Image", path: "Image.URL" }
            ]
        )
        
}